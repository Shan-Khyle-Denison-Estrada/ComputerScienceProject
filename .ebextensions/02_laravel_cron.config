files:
  "/etc/cron.d/laravel_scheduler":
    mode: "000644"
    owner: root
    group: root
    content: |
      * * * * * webapp . /opt/elasticbeanstalk/deployment/env && cd /var/app/current && php artisan schedule:run >> /var/app/current/storage/logs/cron.log 2>&1

commands:
  01_remove_old_cron:
    command: "rm -f /etc/cron.d/laravel_scheduler.bak"
  02_prepare_log_file:
    command: "touch /var/app/current/storage/logs/cron.log && chown webapp:webapp /var/app/current/storage/logs/cron.log"
    ignoreErrors: true